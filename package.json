{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm run start-server",
    "start-server": "npm run start --prefix server",
    "start-dev": "concurrently \"npm run start --prefix client\" \"npm run start --prefix server\"",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
